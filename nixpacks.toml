[phases.setup]
nixPkgs = ["python310", "pip", "libGL"]

[phases.install]
cmds = [
  "python -m ensurepip --upgrade",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.start]
cmd = "python app.py"
